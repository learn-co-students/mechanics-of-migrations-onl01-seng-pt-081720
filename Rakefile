require_relative './config/environment'
require 'sinatra/activerecord/rake'

desc 'Here we open the console'
task :console do
  require 'irb'
  ARGV.clear
  IRB.start
end



#Sorry I type genra instead of genre and I change the Spec